import { GroepFilterPipe } from './groep-filter.pipe';

describe('GroepFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GroepFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
